'use client'; import type { InheritedComponentProps } from '../types'; 
import type { ReactNode } from 'react';
import { forwardRef } from 'react';
import type { TextInputProps } from 'react-native';
import { Text, TextInput, View } from 'react-native';
import { cn } from '../core/cn';
/** Props for a labeled text input with adornments and validation feedback. */ export type TextFieldProps = InheritedComponentProps<Omit<TextInputProps, 'children'> & { className?: string; endAdornment?: ReactNode; error?: boolean; helperText?: string; label?: string; startAdornment?: ReactNode; variant?: 'filled' | 'outlined' | 'standard'; }>;
/** Renders an accessible text input with consistent cross-platform styling. */ export const TextField = forwardRef<TextInput, TextFieldProps>(function TextField({ className, endAdornment, error, helperText, label, multiline, placeholderTextColor = '#94A3B8', startAdornment, variant = 'outlined', ...props }, ref) { return (<View className={className}>{label ? <Text className={cn('mb-2 text-sm font-bold text-slate-600 dark:text-slate-300', error && 'text-rose-600 dark:text-rose-300')}>{label}</Text> : null}<View className={cn('min-h-[52px] flex-row items-center px-4 web:transition-all web:duration-200 web:focus-within:border-brand-500 web:focus-within:ring-2 web:focus-within:ring-brand-100 dark:web:focus-within:ring-brand-950', variant === 'outlined' && 'rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-700 dark:bg-slate-900', variant === 'filled' && 'rounded-t-xl border-b-2 border-slate-400 bg-slate-100 dark:border-slate-500 dark:bg-slate-800', variant === 'standard' && 'border-b border-slate-400 px-0 dark:border-slate-500', error && 'border-rose-500 dark:border-rose-400', multiline && 'items-start py-3')}>{startAdornment ? <View className="mr-3">{startAdornment}</View> : null}<TextInput accessibilityLabel={label} className={cn('min-w-0 flex-1 py-3 text-base text-slate-950 web:outline-none dark:text-white', multiline && 'min-h-24 text-left')} multiline={multiline} placeholderTextColor={placeholderTextColor} ref={ref} textAlignVertical={multiline ? 'top' : 'center'} {...props}/>{endAdornment ? <View className="ml-3">{endAdornment}</View> : null}</View>{helperText ? <Text className={cn('mt-1.5 px-1 text-xs text-slate-500 dark:text-slate-400', error && 'text-rose-600 dark:text-rose-300')}>{helperText}</Text> : null}</View>); });

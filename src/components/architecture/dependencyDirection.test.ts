import fs from 'node:fs'; import path from 'node:path'; import ts from 'typescript';
const root = path.resolve(__dirname, '..');
const policies = [{ dir: 'data-display/model', forbidden: ['react', 'react-native', '/presentation/', '/infrastructure/', '/composition/'] }, { dir: 'lazy-loading/ports', forbidden: ['react', 'react-native', '/application/', '/infrastructure/', '/presentation/', '/composition/'] }, { dir: 'lazy-loading/application', forbidden: ['react', 'react-native', '/infrastructure/', '/presentation/', '/composition/'] }, { dir: 'lazy-loading/presentation', forbidden: ['/infrastructure/', '/composition/'] }] as const;
function files(dir: string): string[] { return fs.readdirSync(dir, { withFileTypes: true }).flatMap((entry) => { const file = path.join(dir, entry.name); return entry.isDirectory() ? files(file) : /\.tsx?$/.test(entry.name) && !entry.name.includes('.test.') ? [file] : []; }); }
function imports(file: string) { const tree = ts.createSourceFile(file, fs.readFileSync(file, 'utf8'), ts.ScriptTarget.Latest, true); return tree.statements.filter(ts.isImportDeclaration).map((node) => (node.moduleSpecifier as ts.StringLiteral).text); }
function violations() { return policies.flatMap(({ dir, forbidden }) => files(path.join(root, dir)).flatMap((file) => imports(file).flatMap((specifier) => forbidden.some((term) => specifier === term || specifier.includes(term)) ? [`${dir} -> ${specifier}`] : []))); }
describe('Clean Architecture dependency direction', () => { it('keeps inner layers independent from outer adapters', () => { expect(violations()).toEqual([]); }); });
